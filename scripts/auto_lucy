# To be copied to /etc/
# and activated by adding the following line (without #) to /etc/auto_master:
# /mnt/LucySynology      auto_lucy

# We use /mnt instead of /Volumes as OS X doesn't automatically create a directory for the mountpoint in /Volumes.
# In contrast, /mnt/LucySynology is created when needed.

web -fstype=afp afp://admin:admin@10.0.0.20/web
